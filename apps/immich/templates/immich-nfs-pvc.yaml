kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: immich-pvc-nfs
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: immich-pv-nfs
  storageClassName: nfs-csi